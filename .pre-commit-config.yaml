repos:
  - repo: local
    hooks:
      - id: just-all
        name: Run all checks (format, lint, typehints, complexity)
        entry: just all
        language: system
        pass_filenames: false
        types: [python]
